import{access,readFile}from"node:fs/promises";import{resolve}from"node:path";const root=resolve(import.meta.dirname,"..");const required=["packages/retrieval/src/index.ts","packages/retrieval/tests/retrieval.test.ts","packages/db/migrations/0006_retrieval.sql","packages/db/src/repositories/retrieval.ts","apps/web/app/api/v1/retrieval/index/route.ts","apps/web/app/api/v1/retrieval/search/route.ts","docs/architecture/0013-permission-first-retrieval.md","docs/runbooks/retrieval.md"];await Promise.all(required.map(file=>access(resolve(root,file))));const migration=await readFile(resolve(root,required[2]),"utf8");for(const invariant of ["vector(1536)","source_revision_id","embedding_chunks_select_policy","objects_invalidate_embeddings","current_revision_id=source_revision_id","embedding_chunks_delete_policy"])if(!migration.includes(invariant))throw new Error(`Retrieval migration missing ${invariant}`);const repository=await readFile(resolve(root,required[3]),"utf8");for(const invariant of ["permissions.assert","authorized.map","ANY(${ids}::uuid[])","applyContextBudget","sourceRevisionId"])if(!repository.includes(invariant))throw new Error(`Retrieval repository missing ${invariant}`);console.log(`Retrieval verified (${required.length} required files).`);
