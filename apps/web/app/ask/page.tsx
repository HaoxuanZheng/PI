import Link from"next/link";import{redirect}from"next/navigation";import{getAuthService}from"@/lib/auth";import{AskMyLife}from"./AskMyLife";export default async function AskPage(){if(!await(await getAuthService()).currentUser())redirect("/auth");return<main className="libraryShell"><Link className="back" href="/library">← Private library</Link><p className="eyebrow">Personal Graph intelligence</p><h1 className="compactTitle">Ask My Life</h1><p className="muted">Answers use only evidence you are permitted to read. If the evidence is weak or ambiguous, LifeGraph will ask for clarification instead of guessing.</p><AskMyLife/></main>}
