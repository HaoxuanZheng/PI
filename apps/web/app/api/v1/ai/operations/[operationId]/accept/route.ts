import { z } from "zod";import type { NextRequest } from "next/server";import { apiData,handleApiError,requestId,requireApiContext } from "@/lib/api";import { getAIOperationRepository } from "@/lib/db";
export async function POST(request:NextRequest,{params}:{params:Promise<{operationId:string}>}){const id=requestId(request);try{const ctx=await requireApiContext(request);if(ctx instanceof Response)return ctx;const operationId=z.uuid().parse((await params).operationId);return apiData(await getAIOperationRepository().accept(ctx.actor.id,operationId,ctx.requestId),ctx.requestId);}catch(error){return handleApiError(error,id);}}
